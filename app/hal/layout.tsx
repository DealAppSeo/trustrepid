import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'HAL accuracy — TrustRepID', description: 'Live hallucination-assessment benchmark: precision, recall, F1, and veto history.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
